import { getLocaleDateTimeFormat } from "@angular/common";

export class Configuration {

    keyValue: string = "";
    dataValue: string = "";
    note: string = "";
    system: boolean = false;
    active: boolean = true;
    created!: string;
    updated!: string;
    
}